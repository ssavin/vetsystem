# 🔨 Инструкция по сборке VetSystem Companion

## Требования

- Windows 10/11
- Node.js 18+ (скачать: https://nodejs.org/)
- 2 ГБ свободного места на диске

---

## Шаг 1: Скачайте папку vetsystem-companion

В Replit:
1. Откройте Files (боковая панель)
2. Найдите папку `vetsystem-companion`
3. Правый клик → Download folder

Или скачайте напрямую через Git:
```bash
git clone https://github.com/ваш-репозиторий/vetsystem.git
cd vetsystem/vetsystem-companion
```

---

## Шаг 2: Установите Node.js

1. Перейдите на https://nodejs.org/
2. Скачайте **LTS версию** (рекомендуется)
3. Запустите установщик
4. Следуйте инструкциям (всё по умолчанию OK)
5. **Перезапустите командную строку** после установки

Проверьте установку:
```cmd
node --version
npm --version
```

---

## Шаг 3: Установите зависимости

Откройте командную строку (cmd) в папке `vetsystem-companion`:

```cmd
npm install
```

Подождите 2-5 минут. Warnings о deprecated пакетах можно игнорировать.

---

## Шаг 4: Соберите .exe установщик

```cmd
npm run build:win
```

Сборка займёт 5-10 минут.

---

## Шаг 5: Найдите готовый файл

Установщик будет в папке:
```
vetsystem-companion/release/VetSystem-Companion-Setup-1.0.0.exe
```

---

## ⚠️ Возможные ошибки

### "npm не является командой"

**Решение:** Node.js не установлен или нужно перезапустить командную строку.

1. Закройте и откройте cmd заново
2. Проверьте: `node --version`
3. Если не помогло → переустановите Node.js

---

### "Python is not set"

**Решение:** Нужен Visual Studio Build Tools для better-sqlite3.

```cmd
npm install --global windows-build-tools
```

Или скачайте: https://visualstudio.microsoft.com/downloads/
→ Build Tools for Visual Studio

---

### "Ошибка при сборке better-sqlite3"

**Решение:** Используйте версию Node.js 18 LTS (не 20+).

Скачайте Node.js 18: https://nodejs.org/dist/v18.19.0/node-v18.19.0-x64.msi

---

## ✅ Готово!

Теперь у вас есть:
- `VetSystem-Companion-Setup-1.0.0.exe` - установщик для клиник

Раздайте этот файл клиникам. Они установят, введут URL сервера и API ключ, и смогут работать офлайн!

---

## 🔐 Настройка сервера

**На сервере vetsystemai.ru** нужно добавить в `.env`:

```bash
COMPANION_API_KEY=companion-api-key-2025
```

И перезапустить сервер:
```bash
pm2 restart vetsystem
```

---

## 📞 Поддержка

Если что-то не работает - опишите ошибку, приложите скриншот.
