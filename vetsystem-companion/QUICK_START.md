# 🚀 Быстрый старт VetSystem Companion

## Для разработчиков

### 1. Установка зависимостей

```bash
cd vetsystem-companion
npm install
```

### 2. Запуск в режиме разработки

```bash
npm run dev
```

Это запустит:
- Vite dev server на http://localhost:5173
- Electron приложение с hot reload

### 3. Сборка установщика

```bash
# Windows
npm run build:win

# Mac
npm run build:mac

# Linux  
npm run build:linux
```

Установщик появится в папке `release/`

---

## Настройка сервера

### Добавьте переменную окружения на сервере:

```bash
COMPANION_API_KEY=companion-api-key-2025
```

### Перезапустите сервер:

```bash
pm2 restart vetsystem
```

---

## Первый запуск приложения

1. **Запустите Companion**
2. **Перейдите в Настройки**
3. **Введите**:
   - URL: `https://vetsystemai.ru`
   - API Key: `companion-api-key-2025`
4. **Нажмите "Синхронизировать"**

✅ Готово! Приложение скачает данные и готово к работе.

---

## Тестирование офлайн-режима

1. Отключите интернет
2. Создайте клиента, пациента или счет
3. Индикатор покажет: "Ожидает синхронизации: N"
4. Включите интернет
5. Данные автоматически отправятся на сервер

---

## Структура базы данных

SQLite база находится в:
- Windows: `%APPDATA%\vetsystem-companion\vetsystem.db`
- Mac: `~/Library/Application Support/vetsystem-companion/vetsystem.db`
- Linux: `~/.config/vetsystem-companion/vetsystem.db`

Таблицы:
- `clients` - клиенты
- `patients` - пациенты
- `nomenclature` - прайс-лист
- `appointments` - записи на прием
- `invoices` - счета
- `sync_queue` - очередь синхронизации ⚠️

---

## Отладка

### Просмотр DevTools

В режиме разработки DevTools откроется автоматически.

### Логи синхронизации

Все действия синхронизации логируются в консоль:
```
✓ Sync: Sent initial data (150 clients, 420 patients, 85 nomenclature)
✓ Sync: Processed 5 actions (5 successful)
```

### Очистка базы

Если нужно начать с чистого листа:
1. Закройте приложение
2. Удалите файл `vetsystem.db`
3. Запустите снова
4. Синхронизируйте

---

## Troubleshooting

### Ошибка "Неверный API ключ"
Проверьте, что:
- API ключ правильный
- Переменная `COMPANION_API_KEY` установлена на сервере
- Сервер перезапущен после добавления переменной

### Синхронизация не работает
Проверьте:
- Интернет соединение
- URL сервера правильный
- Сервер доступен (откройте в браузере)
- Проверьте логи в DevTools

### Приложение не запускается
1. Удалите `node_modules/`
2. Удалите `package-lock.json`
3. Запустите `npm install` снова
4. Попробуйте `npm run dev`

---

## 📝 Полезные команды

```bash
# Разработка
npm run dev              # Запуск в dev режиме
npm run build            # Сборка production

# Сборка установщиков
npm run build:win        # Windows .exe
npm run build:mac        # Mac .dmg
npm run build:linux      # Linux AppImage

# Очистка
rm -rf dist release      # Удалить артефакты сборки
rm -rf node_modules      # Удалить зависимости
```

---

## 🎯 Что дальше?

- [ ] Добавьте иконку в `build/icon.ico` (Windows)
- [ ] Настройте автообновление (electron-updater)
- [ ] Добавьте печать документов
- [ ] Интеграция с фискальными принтерами

---

**Готово!** Теперь у вас есть полноценное офлайн-приложение для VetSystem! 🎉
