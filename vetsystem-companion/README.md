# VetSystem Companion

**Десктопное приложение для работы в офлайн-режиме с системой VetSystem**

## 📋 Описание

VetSystem Companion — это легковесное десктопное приложение для Windows, Mac и Linux, которое позволяет продолжать работу ветеринарной клиники даже при отсутствии или нестабильности интернет-соединения.

### Основные возможности:

- ✅ **Работа без интернета** — создавайте клиентов, пациентов, записи на прием и счета офлайн
- 🔄 **Автоматическая синхронизация** — все изменения автоматически отправляются на сервер при появлении соединения
- 📊 **Индикатор статуса** — всегда знайте, сколько записей ожидают синхронизации
- 💾 **Локальная база SQLite** — все данные хранятся на вашем компьютере
- 🎨 **Знакомый интерфейс** — тот же дизайн, что и в веб-версии VetSystem

---

## 🚀 Установка

### Для пользователей (Windows)

1. Скачайте установщик `VetSystem-Companion-Setup-1.0.0.exe`
2. Запустите установщик и следуйте инструкциям
3. При первом запуске введите:
   - **URL сервера**: `https://vetsystemai.ru`
   - **API ключ**: получите у администратора системы

### Для разработчиков

```bash
# Клонируйте репозиторий
cd vetsystem-companion

# Установите зависимости
npm install

# Запуск в режиме разработки
npm run dev

# Сборка установщика для Windows
npm run build:win

# Сборка для Mac
npm run build:mac

# Сборка для Linux
npm run build:linux
```

---

## 🎯 Использование

### Первый запуск

1. **Запустите приложение**
2. **Настройте подключение** (Настройки → Подключение к серверу)
3. **Нажмите "Синхронизировать"** — скачаются клиенты и прайс-лист

### Работа в офлайн-режиме

При пропадании интернета:
- 🟡 Индикатор станет **желтым** — вы в офлайн-режиме
- ✍️ Продолжайте работать — все записи сохраняются локально
- 📊 Счетчик показывает количество несинхронизированных записей

При появлении интернета:
- 🟢 Индикатор станет **зеленым**
- 🔄 Синхронизация произойдет автоматически
- ✅ Все данные окажутся на сервере

---

## 📚 Структура приложения

```
vetsystem-companion/
├── src/
│   ├── main/              # Electron main process
│   │   ├── index.ts       # Точка входа
│   │   ├── database.ts    # SQLite manager
│   │   ├── sync-service.ts # Синхронизация
│   │   └── preload.ts     # IPC bridge
│   ├── renderer/          # React UI
│   │   ├── components/    # Компоненты
│   │   ├── pages/         # Страницы
│   │   └── App.tsx
│   └── shared/
│       └── types.ts       # Общие типы
├── package.json
├── vite.config.ts
└── electron-builder.yml
```

---

## 🔧 Технологии

- **Electron** — кроссплатформенность
- **React + TypeScript** — UI
- **SQLite** — локальная БД
- **Vite** — сборка
- **TanStack Query** — кэширование
- **Axios** — API запросы

---

## 🔒 Безопасность

- 🔐 API ключ хранится зашифрованным
- 📁 База данных хранится локально в `AppData/VetSystem`
- 🌐 HTTPS соединение с сервером
- ✅ Валидация всех данных перед отправкой

---

## 📡 API Endpoints

### GET /api/sync/initial-data
Скачивание начальных данных (клиенты, пациенты, прайс-лист)

**Headers:**
```
X-API-Key: companion-api-key-2025
```

**Response:**
```json
{
  "clients": [...],
  "patients": [...],
  "nomenclature": [...]
}
```

### POST /api/sync/upload-changes
Отправка локальных изменений на сервер

**Headers:**
```
X-API-Key: companion-api-key-2025
Content-Type: application/json
```

**Body:**
```json
{
  "tenantId": "1",
  "branchId": "1",
  "actions": [
    {
      "queue_id": 1,
      "action_type": "create_client",
      "payload": { ... }
    }
  ]
}
```

**Response:**
```json
{
  "results": [
    {
      "queue_id": 1,
      "status": "success",
      "server_id": 123
    }
  ]
}
```

---

## 🐛 Отладка

### Просмотр логов

В режиме разработки:
- DevTools откроется автоматически
- Логи синхронизации в консоли

### Расположение базы данных

- **Windows**: `C:\Users\<User>\AppData\Roaming\vetsystem-companion\vetsystem.db`
- **Mac**: `~/Library/Application Support/vetsystem-companion/vetsystem.db`
- **Linux**: `~/.config/vetsystem-companion/vetsystem.db`

### Сброс базы данных

Удалите файл `vetsystem.db` и перезапустите приложение

---

## 🔄 Процесс синхронизации

### Сверху вниз (Server → Companion):
1. Приложение запрашивает `GET /api/sync/initial-data`
2. Сервер отдает всех клиентов, пациентов и номенклатуру
3. Приложение **полностью заменяет** локальный прайс-лист

### Снизу вверх (Companion → Server):
1. При создании клиента/счета — запись добавляется в `sync_queue`
2. Каждую минуту (или вручную) приложение проверяет интернет
3. Если онлайн — отправляет `POST /api/sync/upload-changes`
4. Сервер обрабатывает каждую запись и возвращает результат
5. Приложение обновляет статус в `sync_queue`

---

## 📝 Changelog

### Version 1.0.0 (2025-01-XX)
- ✨ Первый релиз
- ✅ Работа с клиентами и пациентами
- ✅ Создание записей на прием
- ✅ Выставление счетов
- ✅ Двусторонняя синхронизация

---

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте URL сервера и API ключ в настройках
2. Убедитесь, что основной сервер VetSystem доступен
3. Проверьте логи в DevTools (F12 в режиме разработки)

---

## 📄 Лицензия

Proprietary. © 2025 VetSystem. All rights reserved.
